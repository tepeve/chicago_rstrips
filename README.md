# chicago-rstrips

Proyecto ETL para extraer trips desde Socrata y almacenarlos en bases postgre local (dev) y redshift (prod).


Proyecto ETL para extraer trips desde Socrata y almacenarlos en bases postgre local (dev) y redshift (prod).

## 🧪 Testing

```bash
# Tests unitarios (rápidos, sin BD)
pytest -m "unit and ci"
```